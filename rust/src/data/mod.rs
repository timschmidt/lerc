//! Shared data model, error, and decoded-value types.

/// Typed decoded data containers and conversion helpers.
pub mod decoded;
/// Shared scalar data types, encode specs, errors, and result aliases.
pub mod types;
